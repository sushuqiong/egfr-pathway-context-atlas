# 图表制作规范（v11，强制）— 防止版面问题复发

> 背景：v9/v10 图件多次出现字号过小、标签压叠、旧版残留、面板重复、标题被裁等问题。本规范为后续任何图件修改的**检查清单**，机器验证见 `reruns/scripts/112_ocr_qa_v3.py`。

## 一、物理尺寸与字号（本次问题根因）
1. **任何图宽度不得超过 6.7 in（17.0 cm）**（300 dpi → ≤2010 px）。超过版宽会被期刊缩排，字号随之跌破 7 pt。
   - 检查：`02_图片_TIFF/figure_specs.csv` 的 `cm_w` 必须 ≤17.0。
2. 字号下限（印刷尺寸下成立，不是像素数）：
   - 坐标轴文字 ≥ 9 pt；图例/注释 ≥ 8 pt；图内数值 ≥ 8 pt；标题 ≥ 10 pt（加粗）
   - ggplot：`theme(axis.text=element_text(size=9))` 等用**pt**；`geom_text(size=)` 用**mm**（3.0 mm ≈ 8.5 pt，2.7 mm ≈ 7.7 pt 为下限）
   - graphviz：`fontsize ≥ 12`
3. 多面板图高度按行数设定（例：28 行条形图 → 高 9.4 in）；单图高度 ≤ 11 in。

## 二、内容一致性（本次另一类问题）
4. **所有数字必须来自冻结结果表**（`reruns/results/*.csv`），不得手写；图注中的数字与正文、Table 1 一致。
5. 面板不得重复：一张图内不得同时出现"旧版 A/B 面板 + 新版 B 面板"；文件名与内容必须一一对应（`fig2a_*` 只含 A，`fig2b_*` 只含 B）。
6. **不可估计的格子必须显式标注**（如 `n.e.`），不得留空白造成误解。
7. 行/列标签命名必须与结果表一致（曾出现绘图用 `EPH` 而结果表为 `EPH_RECEPTORS` 导致整行灰掉）；绘图前用统一 `lab()` 映射，且检查映射后无重复水平（否则 `factor()` 报错）。
8. 图注必须与图内容相符（曾出现图注写"含 EGFR/ERBB2 扩增"而图内只有突变）：图注逐项对照面板。

## 三、机器验证流程（每次改图后必跑）
```bash
# 1) 生成图（R）
Rscript reruns/scripts/114_v11_figures_final.R
# 2) 导出 300 dpi TIFF（≤17 cm）
python reruns/scripts/94_tiff_normalise.py
# 3) OCR 版面质检（裁切 / 重叠 / <7pt / 关键标签缺失）
python reruns/scripts/112_ocr_qa_v3.py
```
通过标准：**clipping = 0**，真实文字重叠 = 0（高 <10 px 的 `-`/`+`/`5` 为网格线误读，忽略），
关键标签（情境名、模块名、数字、`n.e.`）OCR 可读；结果写入 `04_评审记录与报告/ocr_figure_qa.csv`。

## 四、当前 v11 图件状态（2026-09-13 复核）
| 图 | 宽×高 (px) | 版宽 (cm) | 裁切 | 真实重叠 | 关键标签 |
|---|---|---|---|---|---|
| Fig1 并行证据层 | 2008×1532 | 17.0 | 0 | 0 | 全部可读 |
| Fig2A raw 热图 | 2010×1680 | 17.0 | 0 | 0 | 情境/模块/数字可读 |
| Fig2B adjusted 热图 | 2010×1680 | 17.0 | 0 | 0 | 含 n.e. 标注 |
| Fig3 base vs adjusted | 2010×1500 | 17.0 | 0 | 0 | 全部可读 |
| Fig4 TCGA forest | 2010×1740 | 17.0 | 0 | 0 | 情境/HR 可读 |
| Fig5 分子（A 扩增/B 突变） | 2010×1920 | 17.0 | 0 | 0 | profiled 分母已标 |
| Fig6 单细胞（A 同类型/B 身份） | 2010×2628 | 17.0 | 0 | 0 | 含配对数 |
| Supp S1 marker vs xCell | 2010×1470 | 17.0 | 0 | 0 | 可读 |
| Supp S2 xCell 残差热图 | 2010×1680 | 17.0 | 0 | 0 | 可读 |

> 说明：OCR 报出的 `<7pt` 计数中，绝大多数是 tesseract 只测量小写字母 x 高度所致（实际字号由代码设定 ≥7.6 pt）；判断字号以本规范第 2 条为准，OCR 仅用于几何类缺陷（裁切/碰撞/缺字）。
